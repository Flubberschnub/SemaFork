services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: semafork
      POSTGRES_PASSWORD: secret
      POSTGRES_USER: myuser
    ports:
      - "5432:5432"
    volumes:
      - semafork-postgres:/var/lib/postgresql/data

volumes:
  semafork-postgres:
